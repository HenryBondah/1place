document.getElementById('attendanceForm').addEventListener('submit', function(event) {
    event.preventDefault();
    // Add logic to save data to Google Sheets
    alert("Attendance saved.");
});

// Function to load attendance checkboxes
function loadAttendanceCheckboxes() {
    // Add logic to load checkboxes
}

// Call loadAttendanceCheckboxes on page load
loadAttendanceCheckboxes();
